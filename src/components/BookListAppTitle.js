//components - BookListAppTitle.js

import React from 'react';

const AppTitle = ({ title }) => {
  return(
    <div className="appTitle">
      <h1>{ title }</h1>
    </div>
  );
}

export default AppTitle;